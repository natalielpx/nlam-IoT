apiVersion: v1
kind: Service
metadata:
  name: part3-service
  namespace: dev
spec:
  type: NodePort
  selector:
      app: part3
  ports:
    - port: 8888
      targetPort: app
      nodePort: 30088
